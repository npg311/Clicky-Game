import React from "react";
import "./style.css";

function Header() {

  return (
    
    <header className="header">
    
      <h1>Clicky Game!</h1>
    <h2>Click on an image to score points but don't click on the same image twice!</h2>
    
    </header>
  );
}

export default Header;
